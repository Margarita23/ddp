<p style="color: green"><%= notice %></p>

<h1>Groups</h1>

<div id="groups">
  <% @groups.each do |group| %>
    <%= render group %>
    <p>
      <%= link_to "Show this group", defense_process_group_path(@defense_process.id, group.id), method: :get %>
    </p>
  <% end %>
</div>

<%= link_to "New group", new_defense_process_group_path %>
