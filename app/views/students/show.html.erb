<p style="color: green"><%= notice %></p>

<%= render partial: "/shared/icon_links_btns", locals: { back: :back, edit: edit_student_path(@student), delete: student_path, eye: false } %>

<%= render @student %>

<div>
  <%= link_to "Open list of students", students_path %>
</div>
