<%= form_with(model: criterium) do |form| %>
  <% if criterium.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(criterium.errors.count, "error") %> prohibited this criterium from being saved:</h2>

      <ul>
        <% criterium.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :eval_method_id, style: "display: block" %>
    <%= form.text_field :eval_method_id %>
  </div>

  <div>
    <%= form.label :expert_id, style: "display: block" %>
    <%= form.text_field :expert_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
