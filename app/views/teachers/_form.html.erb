<%= form_with(model: teacher) do |form| %>
  <% if teacher.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(teacher.errors.count, "error") %> prohibited this teacher from being saved:</h2>

      <ul>
        <% teacher.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form_container">
    <div class="field">
      <%= form.label "Ім'я" %><br />
      <%= form.text_field :first_name, class: "form-control" %>
    </div>

    <div class="field">
      <%= form.label "Прізвище" %><br />
      <%= form.text_field :last_name, class: "form-control" %>
    </div>

    <div class="field">
      <%= form.label "По-батькові" %><br />
      <%= form.text_field :patronymic, class: "form-control" %>
    </div>

    <div class="field">
      <%= form.label "Вчений ступінь" %><br />
      <%= form.text_field :academic_status, class: "form-control" %>
    </div>

    <div class="field">
      <%= form.label "Посада" %><br />
      <%= form.text_field :position, class: "form-control" %>
    </div>

    <%= form.submit "Додати студента", class: "btn btn-success" %>
  </div>

<% end %>
